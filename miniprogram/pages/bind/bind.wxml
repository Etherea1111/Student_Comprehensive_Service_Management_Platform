<view class="page bind-page">
  <view class="page-header">
    <view class="page-title">绑定学生身份</view>
    <view class="page-subtitle">首次注册账号需要绑定本人学号和姓名，绑定成功后可使用账号密码登录。</view>
  </view>

  <view class="bind-card card">
    <view class="form-row">
      <text class="form-label">学号</text>
      <input class="form-input" value="{{studentNo}}" maxlength="10" type="number" placeholder="请输入 10 位数字学号" bindinput="onStudentNoInput" />
    </view>
    <view class="form-row">
      <text class="form-label">姓名</text>
      <input class="form-input" value="{{name}}" placeholder="请输入学生姓名" bindinput="onNameInput" />
    </view>
    <button class="primary-button full-button" bindtap="submitBind">确认绑定</button>
  </view>

  <view class="bind-note card">
    若该学生信息已绑定到更早注册的账号，系统会自动退出并删除当前较新的账号，同时登录至较旧账号。
  </view>
</view>
