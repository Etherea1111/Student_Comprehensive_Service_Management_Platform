# 2026-05-27 开发日志

## 小程序后端接口地址同步

### 工作目标

根据当前课程答辩演示环境，将小程序请求后端地址切换到已部署的服务器地址，方便微信开发者工具和真机调试直接访问后端接口。

### 完成内容

1. 拉取远端最新代码，确认 `main` 分支已同步到最新版本。
2. 修改小程序环境配置：
   - 文件：`miniprogram/config/env.js`
   - 后端地址：`http://39.106.161.170:8080`
3. 保持原有请求封装不变，仅启用 API 地址配置。

### 验证结果

已完成以下检查：

- `node --check miniprogram/config/env.js` 通过。
- `git diff --check` 通过。

### 注意事项

- 当前地址为 HTTP，适合开发者工具调试和课程答辩演示联调。
- 后续提交微信正式审核前，仍需切换为 HTTPS 域名，并在微信公众平台配置合法请求域名。

---

## 补充：数据库口径切换为 PostgreSQL

### 工作目标

根据当前阿里云演示服务器的实际环境，将项目部署和数据库说明从 Kingbase 优先调整为 PostgreSQL 优先，确保后端配置、初始化脚本和联调说明与服务器一致。

### 完成内容

1. 确认后端连接层使用 Node.js `pg` 驱动，代码逻辑天然面向 PostgreSQL 连接字符串。
2. 将 `.env.example` 的数据库示例切换为 PostgreSQL 默认端口 `5432` 和服务器演示库命名。
3. 将 `backend/db/schema.sql` 注释改为 PostgreSQL schema。
4. 更新 `docs/backend-deployment.md`，补充 PostgreSQL 建库、应用用户授权、初始化和验证流程。
5. 更新 `todolist.md`，将 P0-01 从 Kingbase 接入口径调整为 PostgreSQL 接入与迁移验证。

### 验证结果

已完成以下检查：

- 本地 WSL 后端连接 Windows PostgreSQL/Kingbase 兼容测试库时，`npm run db:verify` 已通过。
- 已发现并修正 schema 建表顺序问题：`process_reminder_notifications` 必须在 `announcements` 表之后创建。
- 已修正 `verify-db.js` 中 `work_records` 的旧字段检查，使其与当前 schema 的 `occurred_at` 和 `materials_summary` 保持一致。

### 后续事项

- 阿里云服务器当前 PostgreSQL 连接串为 `postgresql://appuser:***@localhost:5432/studentapp`。
- 服务器登录接口当前报错 `permission denied for table users`，需要在服务器 PostgreSQL 中给 `appuser` 补齐 public schema 下表和序列权限。
